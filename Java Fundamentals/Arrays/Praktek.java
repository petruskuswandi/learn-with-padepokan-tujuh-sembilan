import java.util.Scanner;

public class Praktek {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        String a;
        int b;
        boolean c;
        String d, e;
        // int lengthArr = 0;
        // lengthArr = input.nextInt();
        a = input.next();
        b = input.nextInt();
        c = input.nextBoolean();
        e = input.nextLine();
        d = input.nextLine();

        System.out.println("a: " + a + " " + "b: " + b + " " + "c: "+  c + " " + "d: " + d + " e: " + e);
        // input.nextLine();
        // String hari[] = new String[lengthArr];
        // String inputHari = "";
        // for (int i = 0; i < hari.length; i++) {
        //     inputHari = input.nextLine();
        //     hari[i] = inputHari;
        // }

        // for (String day : hari) {
        //     System.out.println(day + " ");
        // }
    }    
};
